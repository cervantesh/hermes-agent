# V2 preflight attempt 5 disposition

Status: **INVALID — not scored and private only**

The second provenance-bound attempt reconciled every token and duration
component after the receipt fixes. It nevertheless rejected the detached
`ordered_dependency_plan` fixture because a consumer searched outside its
assigned workspace. The scope gate behaved correctly, and the access was not
added to the allow-list.

Repeatedly sampling a scored fixture until it happens to obey a mechanical
preflight would select on model outcome. Subsequent preflight uses two dedicated
trivial echo fixtures that exercise the same detached/shared lifecycle and
integrity mechanisms but are excluded from `TASKS` and every scored cohort.
The real pilot fixtures, instructions, outcome gates, and schedule remain
unchanged; any later scope expansion remains an adverse measured outcome.
